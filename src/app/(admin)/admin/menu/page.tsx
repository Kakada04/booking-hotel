import { AdminMenuView } from "@/features/admin/menu/components/admin-menu-view";

export const metadata = {
  title: "Menu — SrokHotel PMS",
  description: "All modules and quick actions for the SrokHotel admin portal.",
};

export default function AdminMenuPage() {
  return <AdminMenuView />;
}
